# Database Connection
# Format: postgres://USER:PASSWORD@HOSTNAME:PORT/DATABASENAME
DATABASE_URL=
DATABASE_SSL=false
DB_NAME=
DB_USER=
DB_PASSWORD=

PGHOST=
PGPORT=
PGUSER=
PGPASSWORD=
PGDATABASE=

# Server Configuration
PORT=
NODE_ENV=

# JWT Configuration
JWT_SECRET=
JWT_EXPIRES_IN=7d

# MailerSend Configuration (for email verification)
MAILERSEND_API_KEY=
FROM_EMAIL=
FROM_NAME=
CONTACT_EMAIL=

# AI Configuration
AI_BASE_URL=
AI_MODEL=
AI_ACCESS_TOKEN=
AI_SERVICE=

# MCP AI Configuration
MCP_BASE_URL=
MCP_LOGIN_PATH=/login
MCP_TOOLS_PATH=/mcp
MCP_USERNAME=
MCP_PASSWORD=
MCP_LOGIN_BODY_JSON=
MCP_JWT=
MCP_TOOL_CATEGORIZE_TRANSACTIONS=categorize_transactions
MCP_TOOL_GENERATE_BUDGET_RECOMMENDATIONS=generate_budget_recommendations
MCP_TOOL_GENERATE_HISTORY_PATTERNS=generate_history_patterns
MCP_TOOL_GENERATE_GLOBAL_ADVICE=generate_global_advice

# Observability: Prometheus scrape endpoint and Grafana Loki log export
SERVICE_NAME=budgetbuddy-api
OTEL_SERVICE_NAME=budgetbuddy-api
OBSERVABILITY_ENV=production
PROMETHEUS_METRICS_PATH=/metrics
PROMETHEUS_METRICS_BEARER_TOKEN=
GRAFANA_LOKI_URL=
GRAFANA_LOKI_USERNAME=
GRAFANA_LOKI_PASSWORD=
LOG_LEVEL=info

# File Upload Configuration
UPLOADS_DIR=
MAX_FILE_SIZE=

# Encryption key
ENCRYPTION_KEY=
